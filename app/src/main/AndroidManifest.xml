<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:supportsRtl="true">

        <receiver
            android:name="org.fcitx.rimesync.SyncPathReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="org.fcitx.rimesync.STORE_PATHS" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
